class Question {
  String questionText;
  bool questionAnswer;
// its class constractor
  Question({String q, bool a}) {
    questionText = q;
    questionAnswer = a;
  }
}

// Question newQuestion = Question('text', true);
